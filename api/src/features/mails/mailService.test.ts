import mailService from './mailService'
import { user4 } from '../users/user.fixture'

describe('Test mailService', () => {
  test('Send greeting email', async () => {
    /* const sentEmail = await mailService.sendGreetingEmail(user4.email, '')
    expect(sentEmail[0].statusCode).toBe(202) */
  })
})
